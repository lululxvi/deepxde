# Sphinx + extensions
sphinx>=7.0,<9.0
sphinx-rtd-theme
sphinx-copybutton

# Light scientific deps (used by examples / plots)
matplotlib
numpy
scipy
scikit-learn
scikit-optimize>=0.9.0

# Heavy backends are mocked in docs/conf.py and are NOT required to build docs:
# tensorflow>=2.7.0
# tensorflow-probability[tf]>=0.11.0
# torch
# paddlepaddle>=2.6.2
# jax
# flax
# optax
